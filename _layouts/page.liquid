---
layout: default
---
{% if page._styles %}
  <!-- Page/Post style -->
  <style type="text/css">
    {{ page._styles }}
  </style>
{% endif %}

<div class="post">
  <header class="post-header">
    <h1 class="post-title">{{ page.title }}</h1>
    <p class="post-description">{{ page.description }}</p>
  </header>

  {% if page.collection == 'projects' and page.img %}
    <div class="project-hero">
      {% include figure.liquid loading="eager" path=page.img alt=page.title class="project-hero__image" %}
    </div>
  {% endif %}

  {% if page.collection == 'projects' and page.figure_captions %}
    <div class="project-figure-captions">
      <p class="project-figure-captions__title">Figure captions:</p>
      <ul>
        {% for caption in page.figure_captions %}
          <li>{{ caption }}</li>
        {% endfor %}
      </ul>
    </div>
  {% endif %}

  <article>
    {{ content }}
  </article>

  {% if page.related_publications %}
    <h2>References</h2>
    <div class="publications">
      {% bibliography --cited_in_order %}
    </div>
  {% endif %}

  {% if site.giscus and page.giscus_comments %}
    {% include giscus.liquid %}
  {% endif %}
</div>
